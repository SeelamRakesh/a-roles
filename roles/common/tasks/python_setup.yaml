- name: Install python packages
  ansible.builtin.dnf:
   name: "{{ item }}"
   state: present
  loop: 
   - python3 
   - gcc 
   - python3-devel

- name: Install Python libraries
  ansible.builtin.pip:
    name: requirements.txt
    executable: pip3.9
    args:
      chdir: /app